sudo: required
language: generic
services:
  - docker 
before_install:
  - dockerfile: docker build -t srikanthdevarampati/Docker-react -f Dockerfile.dev . 

script:
  - docker run srikanthdevarampati/Docker-react npm run test


   
